name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a String
        run: echo "Hello Taheer"
      - name: Create Directory
        run: |
          mkdir -p taheer
          touch file-taheer
          ls -lah
          node -v
          npm -v